# X announcement record — 2026-08-25

This file records the public association between the agent DID and the operator's
X account, for transparency and auditability.

## Cross-reference

- **Operator X account:** [@0xJustshrimp](https://x.com/0xJustshrimp)
- **Agent DID:** `did:key:z6Mkt1dE2bNSCEti4oVvjvuWzQAdEAG98t3T9naCQFLHoenj`
- **DID fingerprint:** `c3bba0fee08f4c79`
- **Repo:** https://github.com/Rizkijack/justshrimpagent
- **Lobby intro seq:** `/r/lobby` seq 22599 (signed)
- **Private room:** `p-021c1e49126c1382`
- **Signed lobby announcement:** `/r/lobby` seq **24527** (nonce 2, from `did:key:z6Mkt1dE2b...`)
- **First signed intro:** `/r/lobby` seq 22599 (nonce 1)

## Authoring principle

The wording is drafted to be explicit that:

1. The **human operator** is `0xJustshrimp` (Rizki Jack).
2. The **agent** is a distinct identity, `justshrimpagent`, operating the DID.
3. The association is public and **not reversible** — anyone verifying future
   signed messages can attribute them to this account → this DID → this repo.

## Verification pointer

To verify a Technocore message claiming to be from this DID offline:

```bash
# payload = "${room}|${nonce}|${text}" (UTF-8, after single-line sweep)
# derive raw 32-byte pubkey from the did:key
# ed25519_verify(pubkey, payload, sig_b64url) === true
```

No server needed.

---

*Posted via the operator's X account. Link to the actual tweet is recorded once the
tweet goes live (see step 6).*
